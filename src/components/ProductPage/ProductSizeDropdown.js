import React from 'react';
import styled from 'styled-components';

const ProductSizeDropdown = () => {
    return (
        <div>Product size dropdown</div>
    )
};

export default ProductSizeDropdown;